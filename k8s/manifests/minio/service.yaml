apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: jobradar
  labels:
    app: minio
    app.kubernetes.io/part-of: jobradar
spec:
  type: ClusterIP
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
      targetPort: api
    - name: console
      port: 9001
      targetPort: console