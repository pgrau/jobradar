apiVersion: v1
kind: Service
metadata:
  name: embedder
  namespace: jobradar
  labels:
    app: embedder
    app.kubernetes.io/name: embedder
    app.kubernetes.io/part-of: jobradar
spec:
  type: ClusterIP
  selector:
    app: embedder
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
